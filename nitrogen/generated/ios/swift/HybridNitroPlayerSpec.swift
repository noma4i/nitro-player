///
/// HybridNitroPlayerSpec.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules

/// See ``HybridNitroPlayerSpec``
public protocol HybridNitroPlayerSpec_protocol: HybridObject {
  // Properties
  var source: (any HybridNitroPlayerSourceSpec) { get }
  var eventEmitter: (any HybridNitroPlayerEventEmitterSpec) { get }
  var playbackState: PlaybackState { get }
  var memorySnapshot: MemorySnapshot { get }
  var status: NitroPlayerStatus { get }
  var duration: Double { get }
  var volume: Double { get set }
  var currentTime: Double { get set }
  var bufferDuration: Double { get }
  var bufferedPosition: Double { get }
  var muted: Bool { get set }
  var loop: Bool { get set }
  var rate: Double { get set }
  var mixAudioMode: MixAudioMode { get set }
  var ignoreSilentSwitchMode: IgnoreSilentSwitchMode { get set }
  var playInBackground: Bool { get set }
  var playWhenInactive: Bool { get set }
  var isPlaying: Bool { get }
  var isBuffering: Bool { get }
  var isReadyToDisplay: Bool { get }

  // Methods
  func replaceSourceAsync(source: (any HybridNitroPlayerSourceSpec)) throws -> Promise<Void>
  func clearSourceAsync() throws -> Promise<Void>
  func release() throws -> Void
  func initialize() throws -> Promise<Void>
  func preload() throws -> Promise<Void>
  func play() throws -> Void
  func pause() throws -> Void
  func seekBy(time: Double) throws -> Void
  func seekTo(time: Double) throws -> Void
}

public extension HybridNitroPlayerSpec_protocol {
  /// Default implementation of ``HybridObject.toString``
  func toString() -> String {
    return "[HybridObject NitroPlayer]"
  }
}

/// See ``HybridNitroPlayerSpec``
open class HybridNitroPlayerSpec_base {
  private weak var cxxWrapper: HybridNitroPlayerSpec_cxx? = nil
  public init() { }
  public func getCxxWrapper() -> HybridNitroPlayerSpec_cxx {
  #if DEBUG
    guard self is any HybridNitroPlayerSpec else {
      fatalError("`self` is not a `HybridNitroPlayerSpec`! Did you accidentally inherit from `HybridNitroPlayerSpec_base` instead of `HybridNitroPlayerSpec`?")
    }
  #endif
    if let cxxWrapper = self.cxxWrapper {
      return cxxWrapper
    } else {
      let cxxWrapper = HybridNitroPlayerSpec_cxx(self as! any HybridNitroPlayerSpec)
      self.cxxWrapper = cxxWrapper
      return cxxWrapper
    }
  }
}

/**
 * A Swift base-protocol representing the NitroPlayer HybridObject.
 * Implement this protocol to create Swift-based instances of NitroPlayer.
 * ```swift
 * class HybridNitroPlayer : HybridNitroPlayerSpec {
 *   // ...
 * }
 * ```
 */
public typealias HybridNitroPlayerSpec = HybridNitroPlayerSpec_protocol & HybridNitroPlayerSpec_base
