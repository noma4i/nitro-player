///
/// HybridNitroPlayerViewManagerSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `HybridNitroPlayerSpec` to properly resolve imports.
namespace margelo::nitro::video { class HybridNitroPlayerSpec; }
// Forward declaration of `ResizeMode` to properly resolve imports.
namespace margelo::nitro::video { enum class ResizeMode; }
// Forward declaration of `SurfaceType` to properly resolve imports.
namespace margelo::nitro::video { enum class SurfaceType; }
// Forward declaration of `ListenerSubscription` to properly resolve imports.
namespace margelo::nitro::video { struct ListenerSubscription; }

#include <memory>
#include "HybridNitroPlayerSpec.hpp"
#include <optional>
#include "ResizeMode.hpp"
#include "SurfaceType.hpp"
#include "ListenerSubscription.hpp"
#include <functional>

namespace margelo::nitro::video {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NitroPlayerViewManager`
   * Inherit this class to create instances of `HybridNitroPlayerViewManagerSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNitroPlayerViewManager: public HybridNitroPlayerViewManagerSpec {
   * public:
   *   HybridNitroPlayerViewManager(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNitroPlayerViewManagerSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNitroPlayerViewManagerSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNitroPlayerViewManagerSpec() override = default;

    public:
      // Properties
      virtual std::optional<std::shared_ptr<HybridNitroPlayerSpec>> getPlayer() = 0;
      virtual void setPlayer(const std::optional<std::shared_ptr<HybridNitroPlayerSpec>>& player) = 0;
      virtual bool getControls() = 0;
      virtual void setControls(bool controls) = 0;
      virtual ResizeMode getResizeMode() = 0;
      virtual void setResizeMode(ResizeMode resizeMode) = 0;
      virtual bool getKeepScreenAwake() = 0;
      virtual void setKeepScreenAwake(bool keepScreenAwake) = 0;
      virtual SurfaceType getSurfaceType() = 0;
      virtual void setSurfaceType(SurfaceType surfaceType) = 0;

    public:
      // Methods
      virtual void enterFullscreen() = 0;
      virtual void exitFullscreen() = 0;
      virtual ListenerSubscription addOnFullscreenChangeListener(const std::function<void(bool /* fullscreen */)>& listener) = 0;
      virtual ListenerSubscription addWillEnterFullscreenListener(const std::function<void()>& listener) = 0;
      virtual ListenerSubscription addWillExitFullscreenListener(const std::function<void()>& listener) = 0;
      virtual void clearAllListeners() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NitroPlayerViewManager";
  };

} // namespace margelo::nitro::video
